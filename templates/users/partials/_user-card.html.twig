<div class="card-box" style="min-height: 200px; width: 100%">
    <h4 class="header-title m-t-0 m-b-30 text-center">
        <a href="javascript:void(0);">{{ user.fullname }}</a>
    </h4>
    <div class="text-center">
        <a href="javascript:void(0);">
            <div style="min-height: 75px;">
                <img src="{{ asset(avatars_url_segment ~ user.avatar) }}" class="card-img-top" alt="{{ user.fullname }}"
                     width="100px" height="75px"/>
            </div>
        </a>
    </div>
</div>