<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('app.homepage') }}">{{ app_name }}</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li><a href="javascript: void(0);">{{ t('ui.navbar.menu.item.tags')|trans }}</a></li>
                <li><a href="javascript: void(0);">{{ t('ui.navbar.menu.item.playlists')|trans }}</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <form class="navbar-form" role="search" action="#" method="GET">
                        <div class="input-group">
                            <input type="text" class="form-control" placeholder="{{ t('ui.navbar.menu.input.search-count-videos', {'count': 'many'})|trans }}" name="term" id="search-term" autocomplete="off" disabled>
                            <div class="input-group-btn">
                                <button class="btn btn-success waves-effect waves-light" type="submit" disabled>
                                    <i class="fa fa-search"></i>
                                </button>
                            </div>
                        </div>
                    </form>
                </li>
                {% if is_granted('ROLE_USER') %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                           aria-expanded="false">
                            <img src="{{ asset(avatars_url_segment ~ app.user.avatar) }}" alt="{{ app.user.initials }}"
                                 width="25px" height="25px" class="img-circle">
                            &nbsp;{{ app.user.fullname }} <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a href="javascript: void(0);" class="text-danger">{{ t('ui.navbar.menu.item.logout')|trans }}</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li><a href="javascript: void(0);">{{ t('ui.navbar.menu.item.login')|trans }}</a></li>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>